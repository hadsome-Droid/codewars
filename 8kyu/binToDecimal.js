// todo                         Bin to Decimal


//* Завершите функцию, которая преобразует двоичное число(заданное в виде строки)
//*  в десятичное число.


function binToDec(bin) {
   return parseInt(bin, 2);
}

console.log(binToDec('1'))